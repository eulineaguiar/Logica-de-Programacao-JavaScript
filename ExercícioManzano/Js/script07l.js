// l) Somar os quadrados de A, B e C (páginas 25 e 26)
let A = prompt("Digite o valor de A:");
let B = prompt("Digite o valor de B:");
let C = prompt("Digite o valor de C:");
let somaQuadrados = Math.pow(A, 2) + Math.pow(B, 2) + Math.pow(C, 2);
alert("A soma dos quadrados é: " + somaQuadrados);